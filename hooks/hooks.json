{
  "description": "Lifecycle behavior is routed by the global event dispatcher; this plugin must not add host-level command chains.",
  "centralHookRegistration": {
    "mode": "central-only",
    "manifest": "~/.claude/hooks/event-dispatch-manifest.json",
    "plugin": "superpowers"
  },
  "hooks": {}
}
